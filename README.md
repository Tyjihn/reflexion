# Reflexion (Web)

A 2D puzzle/action game written in C++ (SDL2 + OpenGL ES) and compiled to
WebAssembly with [Emscripten](https://emscripten.org/), so it runs in the browser.

## Play it

Open the published link — no install needed:
`https://tyjihn.github.io/reflexion`
